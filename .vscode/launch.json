{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug minixfs-cli (CMake)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": ["test/minixfs.img"],
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "stopAtEntry": false
    }
  ]
}